creation
alpha: inAlpha maxIter: inMaxIter tol: inTol selection: inSelect l1Ratio: l1 normalize: shouldNormalize center: shouldCenter
 	alpha := inAlpha. 
	maxIter := inMaxIter.
	tol := inTol.
	selection := inSelect.
	l1Ratio := l1.
	normalize := shouldNormalize.
	center := shouldCenter .